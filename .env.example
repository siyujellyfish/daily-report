DATABASE_URL=
INGEST_SECRET=
# Phase 3 only: must point to an isolated test database/branch, never Production.
TEST_DATABASE_URL=
# Optional Playwright target; when omitted, Playwright starts local Next.js with TEST_DATABASE_URL.
PLAYWRIGHT_BASE_URL=
# Optional canonical origin; defaults to the current Production domain.
SITE_URL=https://daily-report-tau-gold.vercel.app
